How do we visualise scalar fields $\phi=f(\vec{r})$? One way is to sketch **level sets** (= level curves, level surfaces, iso-surfaces)
These are the curves or surfaces where the scalar field is constant, hence:
$$\phi = constant$$
